//This subclass checks if there is a statement in a node and assigns it to a token if there is.

public abstract class NodeStmt extends Node {

    //This should be taken care of in other programs, however keeping this to ensure that if the other program breaks nothing is lost.
    
    //private NodeAssn assn;

    //public NodeStmt(NodeAssn assn) {
	//this.assn=assn;
    //}

    //public double eval(Environment env) throws EvalException {
	//return assn.eval(env);
    //}

}
